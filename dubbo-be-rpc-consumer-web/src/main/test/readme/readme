1、指定消费端懒加载模式
@Reference(check = false)
================================================================
2、指定dubbo超时时间，并禁用重试
消费端配置：
dubbo.consumer.timeout=10000
dubbo.consumer.retries=0
生产端配置：
dubbo.provider.timeout=10000
ubbo.provider.retries=0
备注：当消费端未设置，生产端的配置会生效
================================================================
3、指定生产端服务协议与暴露端口
dubbo.protocol.name=dubbo
dubbo.protocol.port=20880
备注：所以单台多个生产端，需要不同的端口，消费端无需，因为面向的是生产端
================================================================
4、dubbo 最新版本 2.7.1 会将服务自动注册为静态服务，真坑
================================================================
5、dubbo动态属性服务的删除过程
对于dubbo的动态属性的生产端服务，
zk检测到停止（也就是心跳失败）后，
由于无法确定是否是网络堵塞引起的失败，
等到2 * tickTime ~ 20 * tickTime，也就是 40秒后，
才会确认关闭此次回话，
也就是删除dubbo服务
================================================================
6、
vo 用于 redis 或者 db 传输用





